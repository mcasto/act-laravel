export const POSTER_BASE_URL = "/api/storage/posters/";
// The 600px-wide variant every poster upload also produces — for small,
// grid/thumbnail-sized displays (e.g. the Gallery page's show cards) where
// the full 1200px version would just be wasted bandwidth.
export const POSTER_SM_BASE_URL = "/api/storage/posters-sm/";
